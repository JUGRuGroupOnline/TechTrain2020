Task2

    Порядок cборки:

        ```` npm install ````


    Запуск:

    1) Запустить сервис из первой задачи командой:

    ```` docker run -p 8080:8080 app/hello ````

    2) Выполнить команду:

     ```` node app.js ````

    API методы:

        GET /crash - возвращает строку "Мы сломали", для вызова http://localhost:5000/crash
